services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    environment:
      - API_BASE_URL=http://backend:8000
    depends_on:
      - backend
